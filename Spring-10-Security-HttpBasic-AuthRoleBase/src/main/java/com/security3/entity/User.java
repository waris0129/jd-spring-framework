package com.security3.entity;

import org.springframework.security.core.userdetails.UserDetails;

public class User {

    // implements UserDetail interface
    // Spring security template : UserDetails
        // we need to convert User to UserDetails
}
